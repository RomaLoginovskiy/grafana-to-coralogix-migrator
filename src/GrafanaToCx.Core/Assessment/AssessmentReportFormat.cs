namespace GrafanaToCx.Core.Assessment;

public enum AssessmentReportFormat
{
    /// <summary>Plain text, for a terminal.</summary>
    Text,

    /// <summary>Markdown tables, for pasting into a document or ticket.</summary>
    Markdown
}
